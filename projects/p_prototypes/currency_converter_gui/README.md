# Currency Converter GUI

A simple currency converter with a modern, dark-themed GUI in Python using `ttk` and the `requests` library.

## Requirements
- `requests`

## Usage

```bash
python main.py
```
