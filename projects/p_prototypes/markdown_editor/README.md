# Markdown Editor

A simple markdown editor in Python with a modern, dark-themed GUI using `ttk` and `markdown2`.

## Requirements
- `markdown2`

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install markdown2
```

## Usage

```bash
python main.py
```
