# Currency Converter

A simple command-line currency converter in Python that uses the `requests` library to fetch data from `api.exchangerate-api.com`.

**Usage:**

```bash
python main.py <base_currency> <target_currency> <amount>
```
