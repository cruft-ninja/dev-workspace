# Chat App

A simple command-line chat application in Python using the `socket` and `threading` libraries.

**Usage:**

1.  Run the server:
    ```bash
    python server.py
    ```
2.  Run the client in a separate terminal:
    ```bash
    python client.py
    ```
3.  Type messages in the client terminal and press Enter to send.
